version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: nurax_db
    environment:
      POSTGRES_DB: ${DB_NAME:-nurax_db}
      POSTGRES_USER: ${DB_USER:-nurax_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-nurax_password}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  api:
    build: .
    container_name: nurax_api
    command: bash -c "python manage.py migrate && python init_db.py && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DB_NAME=${DB_NAME:-nurax_db}
      - DB_USER=${DB_USER:-nurax_user}
      - DB_PASSWORD=${DB_PASSWORD:-nurax_password}
      - DB_HOST=db
      - DB_PORT=5432
      - DB_SSLMODE=disable
      - DEBUG=True
      - SECRET_KEY=dev-key-change-in-prod-insecure
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
    depends_on:
      - db
    restart: always

volumes:
  postgres_data:
